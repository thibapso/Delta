import styles from './Footer.module.css';

function Footer() {
    return (
        <footer id="footer" className={styles.footer}>
            
        </footer>
    )
}

export default Footer; 
